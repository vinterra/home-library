Home Library todo:
* fix content load errors
* create unit test for UrlDocument
* create unit test for mimeType parameter in creates
* add unit test for externalUrl type
* add unit test for report template type
* add unit test for query type, in both creation method
* add unit test for report type
* add unit test for find methods
* add unit test for remove string,string  methods
* add unit test for gpod-link type
* add unit test for data area
* add unit test for AquaMaps type
* add unit test for Time Series type
* move the content creation out the item creation (use only the persistence manager a persistence mechanism)
* refactor the import mechanism: create a manager for each request type
* refactor example methods (common abstract class which set the env and provide help)
* create a hl dev build




Adding a new basket item type steps.
1. add the type to BasketItemType enum
2. create the type interface (extending BasketItem)
3. add create method to basket interface
4. add the creation method to workspacearea interface
5. add fs type implementation (make attention to internal clone method)
6. add creation method to fsworkspacearea and fsfactory
7. add create method to fsbasket
8. override the creation method on persistent workspace area
9. add to WorkspaceTreeVisitor





